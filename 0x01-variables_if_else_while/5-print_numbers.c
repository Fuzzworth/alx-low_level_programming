#include <stdio.h>
/**
* main - Program prints digits
*
* Description: Program prints all digits from 0 to 9
*
* Return: 0 if execution has no errors
*/
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d", digit);
	}
	pritnf("\n");
	return (0);
}
